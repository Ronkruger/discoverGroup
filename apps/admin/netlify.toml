[build]
  base = "apps/admin"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  VITE_ADMIN_API_URL = "https://api--discoverGroup.netlify.app"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200